<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--
    =====================================================================
    Base Element Styles
    =====================================================================
    Global styling for fundamental UI components
    -->

    <!-- 
    =====================================================================
    TextBlock Style
    =====================================================================
    Applies to all TextBlock elements in the application
    -->
    <Style TargetType="TextBlock">
        <!-- 
        Text Color and Alignment:
        - Primary text color for readability
        - Vertical centering for consistent layout
        -->
        <Setter Property="Foreground" Value="#333333"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- 
    =====================================================================
    TextBox Style
    =====================================================================
    Standard styling for all text input fields
    -->
    <Style TargetType="TextBox">
        <!-- 
        Input Field Configuration:
        - Internal padding for text spacing
        - Border styling for visual hierarchy
        -->
        <Setter Property="Padding" Value="5"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="#E0E0E0"/>
        
        <!-- 
        Optional Enhancement Points:
        1. Add FocusVisualStyle for accessibility
        2. Include hover state triggers
        3. Add corner radius for rounded inputs
        -->
    </Style>
</ResourceDictionary>